<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartTraffic" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowBackground">@drawable/bg_main_gradient</item>
    </style>

    <style name="Theme.SmartTraffic" parent="Base.Theme.SmartTraffic">
        <item name="android:windowAnimationStyle">@style/SlideAnimation</item>
    </style>
</resources>